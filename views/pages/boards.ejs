<!doctype html>
<html lang="en">

<head>
  <%- include ../partials/head.ejs %>
  <title>Your boards</title>
</head>

<body>
  <%- include ../partials/header.ejs %>

  <div class="container">

    <h2>Welcome <em><%= user.username %></em></h2>
    <p>Here is an overview of all your Glo Boards. The total time tracked for your boards will be displayed here.</p>

    <div class="card table-card">
      <div class="table-responsive">
        <table class="table table-striped table-dark">
          <thead>
            <tr>
              <th scope="col">Board</th>
              <th scope="col">Total Time</th>
              <th scope="col">Tracking?</th>
            </tr>
          </thead>
          <tbody>
            <% boards.forEach( function (board) { %>
            <tr>
              <th scope="row"><a class="highlighted-link" href="/boards/<%= board.id %>"><%= board.name %></a></th>
              <td><%= board.totalTime %></td>
              <% if (board.trackingEnabled) { %>
              <td>&#9989;</td>
              <% } else { %>
              <td>&#10060;</td>
              <% } %>
            </tr>
            <% }); %>
          </tbody>
        </table>
      </div>
    </div>
    <p>Click on a Board for a closer look at it.</p>
  </div>
  <%- include ../partials/footer.ejs %>
</body>

</html>
